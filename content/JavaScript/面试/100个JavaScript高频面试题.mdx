---

type: "Post"
title: "征服面试：100个JavaScript高频面试题深度解析与最佳实践"
date: "2023-01-15"
description: "全面剖析 100 个 JavaScript 高频面试题，涵盖基础语法、闭包、原型链、异步编程、ES6+ 等核心知识点，深入原理，附带实例和最佳实践。"
keywords: "JavaScript, 面试题, 前端开发, 闭包, 原型链, 异步编程, ES6, Promise, Event Loop"
author: "晓龙"
image: "/images/hero/js-interview.jpg"
tags: ["面试", "JavaScript", "前端开发"]
category: "JavaScript"

---

Hey，前端开发者们！✨ 准备下一次技术面试了吗？JavaScript作为前端技术的基石，其深度与广度往往是面试的焦点。本文将为你系统梳理**100个最高频的JS面试题**，从语言核心到现代特性，从原理剖析到实战应用。目标不仅是帮你通过面试，更是构建坚实、深入的知识体系，让你在日常开发中游刃有余。

系好安全带，我们开始这场知识之旅！🚗

## 📖 快速导航
- [基础篇：语言核心](#基础篇语言核心)
- [进阶篇：深入机制](#进阶篇深入机制)
- [ES6+篇：现代语法](#es6篇现代语法)
- [实战篇：场景与应用](#实战篇场景与应用)
- [硬核篇：原理与手写](#硬核篇原理与手写)
- [扩展篇：更深更广](#扩展篇更深更广)
- [性能与优化篇](#性能与优化篇)
- [奇技淫巧篇](#奇技淫巧篇)
- [终极篇：模式与架构](#终极篇模式与架构)
- [实战与边缘篇](#实战与边缘篇)

---

## 基础篇：语言核心

### 1. `var`、`let` 和 `const` 的区别是什么？
三者是声明变量的不同方式，核心区别在于**作用域、提升与可变性**。

- **`var`**：函数作用域，存在变量提升，可重复声明。
- **`let`**：块级作用域，存在暂时性死区（TDZ），不可重复声明。
- **`const`**：块级作用域，声明时必须赋值，且**绑定不可变**（对于引用类型，其内部属性可改）。

```javascript
console.log(a); // undefined (变量提升)
var a = 1;

console.log(b); // ReferenceError (暂时性死区)
let b = 2;

const c = { name: '晓龙' };
c.name = '小明'; // ✅ 允许：修改属性
c = {}; // ❌ TypeError：重新赋值
```

**最佳实践**：默认使用 `const`，需要重新赋值时使用 `let`，基本淘汰 `var`。

---

### 2. 什么是变量提升（Hoisting）？
JavaScript引擎在执行代码前，会将变量和函数的声明"提升"到其作用域的顶部。但需注意：
- `var` 声明的变量提升，值为 `undefined`。
- `let`/`const` 也存在提升，但处于"暂时性死区"，访问会报错。
- 函数声明整体提升。

```javascript
console.log(foo); // undefined
var foo = 'bar';

sayHi(); // "Hi!"
function sayHi() {
  console.log('Hi!');
}
```

---

### 3. `==` 与 `===` 有何不同？
`==` 是**宽松相等**，会进行类型转换；`===` 是**严格相等**，要求类型和值均相同。

```javascript
1 == '1';   // true (类型转换)
1 === '1';  // false
null == undefined;  // true
null === undefined; // false
```

**原则**：几乎总是使用 `===`，除非你明确需要类型转换的逻辑。

---

### 4. `null` 和 `undefined` 的区别？
- **`undefined`**：表示"未定义"。变量声明未赋值、函数无返回值、对象未指定属性时的默认值。
- **`null`**：表示"空值"或"空对象引用"，是一个可以主动赋值的特殊值。

```javascript
let a; // a 为 undefined
let b = null; // b 被显式赋为空
```

---

### 5. 什么是闭包（Closure）？
闭包是指一个函数能够**记住并访问其词法作用域**，即使该函数在其作用域外被执行。

**核心价值**：创建私有变量、实现函数工厂、模块模式。

```javascript
function createCounter() {
  let count = 0; // 私有变量
  return function() {
    count++;
    return count;
  };
}

const counter = createCounter();
console.log(counter()); // 1
console.log(counter()); // 2
```

**注意**：不当使用闭包可能导致内存泄漏，不再需要的闭包应及时解除引用。

---

## 进阶篇：深入机制

### 6. `this` 的绑定规则有哪些？
`this` 的值在函数被调用时确定，遵循以下规则（优先级从低到高）：
1. **默认绑定**：独立函数调用 → `window`（非严格模式）或 `undefined`（严格模式）。
2. **隐式绑定**：作为对象方法调用 → 指向该对象。
3. **显式绑定**：通过 `call`、`apply`、`bind` 指定。
4. **`new` 绑定**：构造函数调用 → 指向新创建的对象。
5. **箭头函数**：继承外层作用域的 `this`，且无法被改变。

```javascript
const obj = {
  name: '晓龙',
  say() { console.log(this.name); },
  sayArrow: () => { console.log(this.name); }
};

obj.say(); // '晓龙' (隐式绑定)
const fn = obj.say;
fn(); // undefined (默认绑定)

obj.sayArrow(); // undefined (箭头函数继承定义时的this)
```

---

### 7. 原型与原型链是如何工作的？
- 每个函数都有一个 `prototype` 属性（原型对象）。
- 每个对象都有一个 `__proto__` 属性（指向其构造函数的原型）。
- 访问对象的属性时，若自身不存在，会沿 `__proto__` 链向上查找，即**原型链**。

```javascript
function Person(name) {
  this.name = name;
}
Person.prototype.sayHello = function() {
  console.log(`Hello, ${this.name}`);
};

const me = new Person('晓龙');
me.sayHello(); // 访问原型方法
console.log(me.__proto__ === Person.prototype); // true
```

---

### 8. Event Loop（事件循环）机制是什么？
JavaScript是单线程的，Event Loop是其处理异步任务的核心机制。
1. 执行所有同步代码（调用栈）。
2. 清空**微任务队列**（如 `Promise.then`、`MutationObserver`）。
3. 取出**宏任务队列**（如 `setTimeout`、`setInterval`、I/O）中的一个任务执行。
4. 重复步骤2-3。

```javascript
console.log('Start');
setTimeout(() => console.log('Timeout'), 0);
Promise.resolve().then(() => console.log('Promise'));
console.log('End');
// 输出：Start -> End -> Promise -> Timeout
```

---

### 9. `Promise` 的核心原理与状态
`Promise` 是一个代表异步操作最终完成或失败的对象。有三种状态：
- `pending`（进行中）
- `fulfilled`（已成功）
- `rejected`（已失败）
状态一旦改变就不可逆。通过 `.then`、`.catch`、`.finally` 进行链式调用。

```javascript
const promise = new Promise((resolve, reject) => {
  setTimeout(() => resolve('Success!'), 1000);
});

promise
  .then(val => console.log(val)) // 'Success!'
  .catch(err => console.error(err));
```

---

### 10. `async/await` 如何简化异步编程？
`async/await` 是基于 `Promise` 的语法糖，让异步代码写法像同步一样直观。
- `async` 函数总是返回一个 `Promise`。
- `await` 会暂停函数执行，等待后面的 `Promise` 完成。

```javascript
async function fetchData() {
  try {
    const response = await fetch('/api/data');
    const data = await response.json();
    console.log(data);
  } catch (error) {
    console.error('Fetch failed:', error);
  }
}
```

---

## ES6+篇：现代语法

### 11. 箭头函数与普通函数的区别
1. **语法更简洁**。
2. **没有自己的 `this`**，继承自外层作用域。
3. **没有 `arguments` 对象**，可使用剩余参数 `...args`。
4. **不能作为构造函数**（不能使用 `new`）。
5. **没有 `prototype` 属性**。

```javascript
const obj = {
  value: 10,
  normal: function() { console.log(this.value); },
  arrow: () => { console.log(this.value); } // this指向全局
};
obj.normal(); // 10
obj.arrow();  // undefined
```

---

### 12. 解构赋值的常见用法
从数组或对象中提取值，并赋值给变量。

```javascript
// 对象解构
const { name, age = 20 } = { name: '晓龙' };
console.log(name, age); // '晓龙', 20

// 数组解构
const [first, , third] = [1, 2, 3];
console.log(first, third); // 1, 3

// 函数参数解构
function greet({ name, greeting = 'Hello' }) {
  return `${greeting}, ${name}!`;
}
```

---

### 13. `Map` 与 `Object` 的主要区别
| 特性 | `Map` | `Object` |
|------|-------|----------|
| 键的类型 | 任意值 | String 或 Symbol |
| 键的顺序 | 插入顺序 | 无序（ES6后部分有序，但不完全可靠） |
| 大小 | `size` 属性 | 手动计算 |
| 性能 | 频繁增删时更优 | 静态键值对时更优 |
| 默认键 | 无 | 从原型链继承 |

```javascript
const map = new Map();
map.set(1, 'one');
map.set({}, 'object');

const obj = {};
obj[1] = 'one'; // 键会被转换为字符串 '1'
```

---

### 14. `Set` 的特性和用途
`Set` 是值的集合，**值唯一**，常用于数组去重、集合运算。

```javascript
const numbers = [1, 2, 2, 3, 4, 4];
const unique = [...new Set(numbers)]; // [1, 2, 3, 4]

const set = new Set([1, 2, 3]);
set.has(2); // true
set.add(4);
set.delete(1);
```

---

### 15. ES6 模块化 (`import`/`export`)
使用 `export` 导出，`import` 导入，实现静态加载，支持树摇（Tree Shaking）。

```javascript
// math.js
export const add = (a, b) => a + b;
export const PI = 3.14159;
export default function multiply(a, b) { return a * b; }

// app.js
import multiply, { add, PI } from './math.js';
console.log(add(2, 3)); // 5
```

---

## 实战篇：场景与应用

### 16. 如何实现深拷贝（Deep Clone）？
深拷贝创建一个新对象，完全复制原对象的所有属性（包括嵌套对象），使两者独立。

**基础递归实现**：
```javascript
function deepClone(obj, hash = new WeakMap()) {
  if (obj === null || typeof obj !== 'object') return obj;
  if (hash.has(obj)) return hash.get(obj); // 处理循环引用
  const cloneObj = Array.isArray(obj) ? [] : {};
  hash.set(obj, cloneObj);
  for (let key in obj) {
    if (obj.hasOwnProperty(key)) {
      cloneObj[key] = deepClone(obj[key], hash);
    }
  }
  return cloneObj;
}
```

**快捷方法**（有局限性）：
```javascript
const cloned = JSON.parse(JSON.stringify(obj));
// 缺点：不支持函数、undefined、循环引用等
```

---

### 17. 防抖（Debounce）与节流（Throttle）的原理与实现
两者都是控制函数执行频率的技术。

**防抖**：事件触发后，等待n秒执行，若n秒内再次触发则重新计时。
```javascript
function debounce(fn, delay) {
  let timer = null;
  return function(...args) {
    clearTimeout(timer);
    timer = setTimeout(() => fn.apply(this, args), delay);
  };
}
```

**节流**：事件触发后，n秒内只执行一次。
```javascript
function throttle(fn, delay) {
  let last = 0;
  return function(...args) {
    const now = Date.now();
    if (now - last >= delay) {
      fn.apply(this, args);
      last = now;
    }
  };
}
```

**应用场景**：
- 防抖：搜索框输入、窗口调整大小
- 节流：滚动事件、按钮点击

---

### 18. 如何处理异步请求中的错误？
**Promise链式调用**：
```javascript
fetch('/api/data')
  .then(response => {
    if (!response.ok) throw new Error('Network error');
    return response.json();
  })
  .then(data => console.log(data))
  .catch(error => console.error('请求失败:', error));
```

**async/await方式**：
```javascript
async function fetchData() {
  try {
    const response = await fetch('/api/data');
    if (!response.ok) throw new Error('Network error');
    const data = await response.json();
    console.log(data);
  } catch (error) {
    console.error('请求失败:', error);
  }
}
```

---

### 19. 如何实现一个简单的发布-订阅模式？
发布-订阅模式是一种对象间一对多的依赖关系。

```javascript
class EventEmitter {
  constructor() {
    this.events = {};
  }

  on(event, listener) {
    if (!this.events[event]) this.events[event] = [];
    this.events[event].push(listener);
  }

  emit(event, ...args) {
    if (this.events[event]) {
      this.events[event].forEach(listener => listener(...args));
    }
  }

  off(event, listenerToRemove) {
    if (this.events[event]) {
      this.events[event] = this.events[event].filter(
        listener => listener !== listenerToRemove
      );
    }
  }
}

// 使用
const emitter = new EventEmitter();
const listener = data => console.log('收到:', data);
emitter.on('message', listener);
emitter.emit('message', 'Hello!');
emitter.off('message', listener);
```

---

### 20. 如何判断一个对象是否为空对象？
空对象是指没有任何自身属性的对象。

```javascript
// 方法1：使用Object.keys
function isEmpty(obj) {
  return Object.keys(obj).length === 0;
}

// 方法2：for...in配合hasOwnProperty
function isEmpty(obj) {
  for (let key in obj) {
    if (obj.hasOwnProperty(key)) return false;
  }
  return true;
}

// 方法3：JSON.stringify
function isEmpty(obj) {
  return JSON.stringify(obj) === '{}';
}

console.log(isEmpty({})); // true
console.log(isEmpty({ a: 1 })); // false
```

---

## 硬核篇：原理与手写

### 21. `new` 操作符的内部执行步骤
`new` 操作符创建一个用户定义的对象类型的实例。

```javascript
function myNew(Constructor, ...args) {
  // 1. 创建新对象，并链接到构造函数的原型
  const obj = Object.create(Constructor.prototype);
  // 2. 执行构造函数，绑定this
  const result = Constructor.apply(obj, args);
  // 3. 判断返回值
  return result instanceof Object ? result : obj;
}

// 使用
function Person(name) {
  this.name = name;
}
const person = myNew(Person, '晓龙');
console.log(person.name); // '晓龙'
```

---

### 22. `call`、`apply` 和 `bind` 的区别与实现
三者都是用于改变函数执行时的 `this` 指向。

```javascript
// call实现
Function.prototype.myCall = function(context, ...args) {
  context = context || window;
  const fnKey = Symbol();
  context[fnKey] = this;
  const result = context[fnKey](...args);
  delete context[fnKey];
  return result;
};

// apply实现
Function.prototype.myApply = function(context, args) {
  context = context || window;
  const fnKey = Symbol();
  context[fnKey] = this;
  const result = context[fnKey](...args);
  delete context[fnKey];
  return result;
};

// bind实现
Function.prototype.myBind = function(context, ...args) {
  const self = this;
  return function(...innerArgs) {
    return self.apply(context, args.concat(innerArgs));
  };
};
```

---

### 23. 如何实现 `Promise.all`？
`Promise.all` 接收一个Promise数组，当所有Promise都成功时返回结果数组，有一个失败则立即失败。

```javascript
function promiseAll(promises) {
  return new Promise((resolve, reject) => {
    const results = [];
    let completed = 0;
    
    promises.forEach((promise, index) => {
      Promise.resolve(promise).then(value => {
        results[index] = value;
        completed++;
        if (completed === promises.length) {
          resolve(results);
        }
      }).catch(reject);
    });
  });
}
```

---

### 24. JavaScript的垃圾回收机制
JavaScript使用自动垃圾回收机制，主要算法有：

1. **标记清除**：从根对象开始，标记所有可达对象，清除未标记对象。
2. **引用计数**：记录对象的引用次数，为0时回收（无法处理循环引用，已较少使用）。

现代浏览器主要使用标记清除及其变种（如分代回收、增量回收等）。

```javascript
// 手动解除引用以帮助垃圾回收
let obj = { data: '...' };
// 使用后...
obj = null; // 解除引用
```

---

### 25. 如何实现一个React中的防抖Hook？
```javascript
import { useState, useEffect } from 'react';

function useDebounce(value, delay) {
  const [debouncedValue, setDebouncedValue] = useState(value);

  useEffect(() => {
    const timer = setTimeout(() => setDebouncedValue(value), delay);
    return () => clearTimeout(timer); // 清除上一次的定时器
  }, [value, delay]);

  return debouncedValue;
}

// 使用示例
function SearchComponent() {
  const [query, setQuery] = useState('');
  const debouncedQuery = useDebounce(query, 500);
  
  useEffect(() => {
    // 根据debouncedQuery发起搜索请求
  }, [debouncedQuery]);
  
  return <input value={query} onChange={e => setQuery(e.target.value)} />;
}
```

---

### 26. `Array.prototype.slice` 和 `splice` 的区别
- **`slice`**：返回新数组，包含从开始到结束（不包括结束）的浅拷贝。
- **`splice`**：修改原数组，通过删除或替换元素来更改数组内容。

```javascript
const arr = [1, 2, 3, 4, 5];
const sliced = arr.slice(1, 3); // [2, 3]
console.log(arr); // [1, 2, 3, 4, 5] 不变

const spliced = arr.splice(1, 2, 'a', 'b'); // 从索引1开始删除2个元素
console.log(arr); // [1, 'a', 'b', 4, 5]
console.log(spliced); // [2, 3]
```

---

### 27. 如何扁平化数组（数组降维）？
```javascript
// 方法1：使用Array.prototype.flat (ES2019)
const arr = [1, [2, [3, [4]]]];
console.log(arr.flat(Infinity)); // [1, 2, 3, 4]

// 方法2：递归实现
function flatten(arr) {
  return arr.reduce((prev, curr) => {
    return prev.concat(Array.isArray(curr) ? flatten(curr) : curr);
  }, []);
}

// 方法3：while循环
function flatten(arr) {
  while (arr.some(item => Array.isArray(item))) {
    arr = [].concat(...arr);
  }
  return arr;
}
```

---

### 28. `forEach` 和 `map` 的区别
- **`forEach`**：遍历数组，对每个元素执行回调，无返回值。
- **`map`**：遍历数组，对每个元素执行回调，返回一个新数组。

```javascript
const arr = [1, 2, 3];
const forEachResult = arr.forEach(x => x * 2); // undefined
const mapResult = arr.map(x => x * 2); // [2, 4, 6]
```

**选择**：需要改变数据并得到新数组时用 `map`，仅需遍历时用 `forEach`。

---

### 29. 如何实现函数柯里化（Currying）？
柯里化是将一个多参数函数转换为一系列单参数函数的技术。

```javascript
function curry(fn) {
  return function curried(...args) {
    // 如果传入的参数数量足够，则执行原函数
    if (args.length >= fn.length) {
      return fn.apply(this, args);
    } else {
      // 否则返回一个新函数，继续接收参数
      return function(...moreArgs) {
        return curried.apply(this, args.concat(moreArgs));
      };
    }
  };
}

// 示例：一个加法函数
function add(a, b, c) {
  return a + b + c;
}
const curriedAdd = curry(add);
console.log(curriedAdd(1)(2)(3)); // 6
console.log(curriedAdd(1, 2)(3)); // 6
```

---

### 30. `Object.create` 的作用是什么？
`Object.create` 创建一个新对象，并使用现有的对象作为新对象的原型。

```javascript
const prototype = { sayHello: function() { console.log('Hello'); } };
const obj = Object.create(prototype);
obj.sayHello(); // Hello
console.log(obj.__proto__ === prototype); // true
```

**应用**：用于实现原型继承，创建纯净对象（`Object.create(null)`）等。

---

## 扩展篇：更深更广

### 31. `setTimeout` 的最小延迟是多少？
浏览器环境下，HTML5规范规定最小延迟是**4ms**。

```javascript
console.log('Start');
setTimeout(() => console.log('Timeout'), 0);
console.log('End');
// 输出：Start -> End -> Timeout（至少延迟4ms）
```

**替代方案**：需要"尽量快"的异步，用 `Promise.resolve().then()`。

---

### 32. 如何准确判断变量类型？
```javascript
// typeof：适合基本类型
typeof null; // "object"（历史遗留问题）

// instanceof：检测构造函数
[] instanceof Array; // true

// Object.prototype.toString：最准确
Object.prototype.toString.call([]); // "[object Array]"
Object.prototype.toString.call(null); // "[object Null]"

// 封装工具函数
function getType(obj) {
  return Object.prototype.toString.call(obj).slice(8, -1).toLowerCase();
}
```

---

### 33. `let` 的 TDZ（时间死区）是什么？
TDZ（Temporal Dead Zone）是 `let` 和 `const` 在声明前不可访问的区域。

```javascript
console.log(a); // ReferenceError: Cannot access 'a' before initialization
let a = 1;
```

---

### 34. 如何实现数组乱序？
使用 **Fisher-Yates 洗牌算法**，保证均匀分布。

```javascript
function shuffle(arr) {
  for (let i = arr.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [arr[i], arr[j]] = [arr[j], arr[i]]; // ES6 解构赋值交换
  }
  return arr;
}
```

---

### 35. `Symbol` 有什么用？
`Symbol` 是唯一的基本类型值，常用于创建私有属性或避免命名冲突。

```javascript
const key = Symbol('key');
const obj = { [key]: 'secret' };
console.log(obj[key]); // "secret"
console.log(Object.keys(obj)); // []（不可枚举）

// 全局Symbol
const globalKey = Symbol.for('globalKey');
console.log(Symbol.for('globalKey') === globalKey); // true
```

---

### 36. `WeakMap` 和 `Map` 的区别？
`WeakMap` 的键是弱引用，键对象无引用时会被垃圾回收；`Map` 的键是强引用。

```javascript
let obj = { name: '晓龙' };
const wm = new WeakMap();
wm.set(obj, 'data');
obj = null; // obj可被回收
```

---

### 37. 如何手写 `reduce`？
```javascript
Array.prototype.myReduce = function(callback, initialValue) {
  let accumulator = initialValue !== undefined ? initialValue : this[0];
  const start = initialValue !== undefined ? 0 : 1;
  
  for (let i = start; i < this.length; i++) {
    accumulator = callback(accumulator, this[i], i, this);
  }
  
  return accumulator;
};

console.log([1, 2, 3].myReduce((a, b) => a + b, 0)); // 6
```

---

### 38. `class` 的本质是什么？
`class` 是构造函数的语法糖，背后还是基于原型继承。

```javascript
class Person {
  constructor(name) {
    this.name = name;
  }
  say() {
    console.log(this.name);
  }
}

// 等价于：
function Person(name) {
  this.name = name;
}
Person.prototype.say = function() {
  console.log(this.name);
};
```

---

### 39. 如何优化大量 DOM 操作？
使用 `DocumentFragment` 批量处理。

```javascript
const frag = document.createDocumentFragment();
for (let i = 0; i < 1000; i++) {
  const div = document.createElement('div');
  div.textContent = i;
  frag.appendChild(div);
}
document.body.appendChild(frag);
```

---

### 40. `requestAnimationFrame` 比 `setTimeout` 好在哪？
`requestAnimationFrame` 按浏览器刷新率（通常60fps）执行，动画更平滑。

```javascript
function animate() {
  console.log('Frame');
  requestAnimationFrame(animate);
}
requestAnimationFrame(animate);
```

---

### 41. 如何实现单例模式？
单例模式确保类只有一个实例。

```javascript
const Singleton = (function() {
  let instance;
  
  function createInstance() {
    return { name: 'Singleton' };
  }
  
  return {
    getInstance: function() {
      if (!instance) instance = createInstance();
      return instance;
    }
  };
})();

const a = Singleton.getInstance();
const b = Singleton.getInstance();
console.log(a === b); // true
```

---

### 42. `Proxy` 有什么用？
`Proxy` 可以拦截对象操作（读、写、删除等），实现响应式数据。

```javascript
const obj = new Proxy(
  {},
  {
    get: (target, key) => `Hello, ${key}`,
    set: (target, key, value) => {
      console.log(`Setting ${key} to ${value}`);
      target[key] = value;
      return true;
    }
  }
);

console.log(obj.name); // "Hello, name"
obj.age = 25; // "Setting age to 25"
```

---

### 43. 如何处理大文件上传？
大文件上传用分片 + 断点续传。

```javascript
const file = new File(['big data'], 'bigfile.txt');
const chunkSize = 1024 * 1024; // 1MB

for (let start = 0; start < file.size; start += chunkSize) {
  const chunk = file.slice(start, start + chunkSize);
  // 上传chunk
}
```

---

### 44. `eval` 有什么风险？
`eval` 执行字符串代码，有安全性风险（XSS）、性能低。

```javascript
eval("console.log('Boom')"); // "Boom"
// 避免使用
```

---

### 45. 如何实现 LRU 缓存？
LRU（最近最少使用）用 `Map`（有序）实现。

```javascript
class LRUCache {
  constructor(capacity) {
    this.capacity = capacity;
    this.cache = new Map();
  }

  get(key) {
    if (!this.cache.has(key)) return -1;
    const value = this.cache.get(key);
    this.cache.delete(key);
    this.cache.set(key, value); // 更新为最近使用
    return value;
  }

  put(key, value) {
    if (this.cache.has(key)) {
      this.cache.delete(key);
    } else if (this.cache.size >= this.capacity) {
      // 删除最久未使用的键（第一个键）
      const oldestKey = this.cache.keys().next().value;
      this.cache.delete(oldestKey);
    }
    this.cache.set(key, value);
  }
}
```

---

### 46. `generator` 函数是啥？
`generator` 是可暂停的函数，用 `yield` 控制流程。

```javascript
function* gen() {
  yield 1;
  yield 2;
  return 3;
}

const it = gen();
console.log(it.next()); // { value: 1, done: false }
console.log(it.next()); // { value: 2, done: false }
console.log(it.next()); // { value: 3, done: true }
```

---

### 47. 如何检测循环引用？
用 `Set` 记录访问过的对象。

```javascript
function hasCycle(obj, seen = new Set()) {
  if (typeof obj !== 'object' || obj === null) return false;
  if (seen.has(obj)) return true;
  seen.add(obj);
  for (let key in obj) {
    if (hasCycle(obj[key], seen)) return true;
  }
  return false;
}

const a = {};
a.b = a;
console.log(hasCycle(a)); // true
```

---

### 48. `Object.defineProperty` 和 `Proxy` 的区别？
- `Object.defineProperty`：定义单个属性，静态拦截。
- `Proxy`：代理整个对象，动态拦截。

```javascript
const obj = {};
Object.defineProperty(obj, 'key', {
  get: () => 'fixed',
  enumerable: true,
  configurable: true
});
console.log(obj.key); // "fixed"
```

---

### 49. 如何实现函数缓存（Memoization）？
用闭包或 `Map` 缓存函数结果。

```javascript
function memoize(fn) {
  const cache = new Map();
  return function(...args) {
    const key = JSON.stringify(args);
    if (cache.has(key)) return cache.get(key);
    const result = fn(...args);
    cache.set(key, result);
    return result;
  };
}

const fib = memoize((n) => n <= 2 ? 1 : fib(n - 1) + fib(n - 2));
console.log(fib(10)); // 55
```

---

### 50. JS的严格模式有啥用？
`"use strict"` 开启严格模式，消除不安全行为。

```javascript
'use strict';
x = 1; // ReferenceError: x is not defined
```

---

## 性能与优化篇

### 51. 如何减少重排（Reflow）和重绘（Repaint）？
重排是布局变化，重绘是样式变化。

```javascript
// 不好：多次重排
el.style.width = '100px';
el.style.height = '100px';

// 好：一次重排
el.style.cssText = 'width: 100px; height: 100px;';

// 更好：用class批量修改
el.classList.add('new-style');
```

---

### 52. 如何实现一个简单的懒加载函数？
懒加载延迟执行函数，直到需要时触发。

```javascript
function lazy(fn) {
  let value;
  let loaded = false;
  
  return function() {
    if (!loaded) {
      value = fn();
      loaded = true;
    }
    return value;
  };
}

const expensive = lazy(() => {
  console.log('Computing...');
  return 42;
});

console.log(expensive()); // "Computing..." 42
console.log(expensive()); // 42
```

---

### 53. `Array.prototype.filter` 如何优化？
大数组用 `for` 循环替代 `filter` 可提升性能。

```javascript
const arr = Array.from({ length: 10000 }, (_, i) => i);

// 慢
const slow = arr.filter(x => x % 2 === 0);

// 快
const fast = [];
for (let i = 0; i < arr.length; i++) {
  if (arr[i] % 2 === 0) fast.push(arr[i]);
}
```

---

### 54. 如何避免回调地狱？
用 `Promise` 链或 `async/await`。

```javascript
// 回调地狱
getData((a) => {
  getMoreData(a, (b) => {
    console.log(b);
  });
});

// 优雅
async function fetchAll() {
  const a = await getData();
  const b = await getMoreData(a);
  console.log(b);
}
```

---

### 55. 如何实现批量异步请求控制并发？
用 `Promise` 和队列限制并发。

```javascript
async function batchRequest(urls, maxConcurrent) {
  const results = [];
  const queue = [...urls];
  
  async function run() {
    while (queue.length) {
      const url = queue.shift();
      try {
        const res = await fetch(url);
        results.push(await res.json());
      } catch (error) {
        results.push({ error });
      }
    }
  }
  
  const workers = Array(Math.min(maxConcurrent, urls.length))
    .fill()
    .map(() => run());
  
  await Promise.all(workers);
  return results;
}
```

---

### 56. 什么是树摇（Tree Shaking）？
树摇是打包工具移除未使用的代码。

```javascript
// util.js
export const used = () => 'used';
export const unused = () => 'unused';

// main.js
import { used } from './util.js';
console.log(used()); // unused被摇掉
```

---

### 57. 如何检测内存泄漏？
用Chrome DevTools的Memory面板。

```javascript
function leak() {
  const arr = [];
  setInterval(() => arr.push(new Array(1000)), 1000); // 内存持续增长
}
```

**预防**：清理定时器和事件监听。

---

### 58. 如何实现一个简单的节流 Promise？
节流加Promise，返回结果。

```javascript
function throttlePromise(fn, delay) {
  let last = 0;
  let promise = null;
  
  return function(...args) {
    const now = Date.now();
    if (now - last >= delay) {
      last = now;
      promise = Promise.resolve(fn(...args));
    }
    return promise;
  };
}
```

---

### 59. `Object.entries` 和 `Object.keys` 的区别？
- `Object.keys`：返回键数组
- `Object.entries`：返回键值对数组

```javascript
const obj = { a: 1, b: 2 };
console.log(Object.keys(obj)); // ["a", "b"]
console.log(Object.entries(obj)); // [["a", 1], ["b", 2]]
```

---

### 60. 如何实现一个简单的防抖 Hook？
React中防抖Hook，优化输入。

```javascript
import { useState, useEffect, useCallback } from 'react';

function useDebounce(callback, delay) {
  const [debouncedCallback, setDebouncedCallback] = useState(() => callback);
  
  useEffect(() => {
    const timer = setTimeout(() => setDebouncedCallback(() => callback), delay);
    return () => clearTimeout(timer);
  }, [callback, delay]);
  
  return debouncedCallback;
}
```

---

## 奇技淫巧篇

### 61. 如何用JS实现链式调用？
返回 `this` 实现链式调用。

```javascript
class Chain {
  constructor() {
    this.value = 0;
  }
  
  add(n) {
    this.value += n;
    return this;
  }
  
  log() {
    console.log(this.value);
    return this;
  }
}

new Chain().add(1).add(2).log(); // 3
```

---

### 62. 如何用一行代码判断回文数？
```javascript
const isPalindrome = (n) => 
  n === Number(n.toString().split('').reverse().join(''));

console.log(isPalindrome(121)); // true
console.log(isPalindrome(123)); // false
```

---

### 63. 如何实现一个简易的模板引擎？
用正则替换。

```javascript
function render(template, data) {
  return template.replace(/\{\{(\w+)\}\}/g, (_, key) => data[key] || '');
}

const html = render('Hello {{name}}!', { name: '晓龙' });
console.log(html); // "Hello 晓龙!"
```

---

### 64. 如何用JS实现加权随机？
用累加权重。

```javascript
function weightedRandom(items) {
  const total = items.reduce((sum, [_, weight]) => sum + weight, 0);
  const rand = Math.random() * total;
  let sum = 0;
  
  for (const [item, weight] of items) {
    sum += weight;
    if (rand <= sum) return item;
  }
}

console.log(weightedRandom([['A', 1], ['B', 2], ['C', 3]]));
```

---

### 65. 如何实现一个简单的Undo/Redo？
用栈记录状态。

```javascript
class History {
  constructor() {
    this.undoStack = [];
    this.redoStack = [];
  }
  
  add(state) {
    this.undoStack.push(state);
    this.redoStack = [];
  }
  
  undo() {
    if (this.undoStack.length) {
      const state = this.undoStack.pop();
      this.redoStack.push(state);
      return this.undoStack[this.undoStack.length - 1];
    }
  }
  
  redo() {
    if (this.redoStack.length) {
      const state = this.redoStack.pop();
      this.undoStack.push(state);
      return state;
    }
  }
}
```

---

### 66. 如何用JS判断设备类型？
解析 `navigator.userAgent`。

```javascript
const isMobile = /Mobi|Android|iPhone/i.test(navigator.userAgent);
console.log(isMobile); // true或false
```

---

### 67. 如何实现一个简易的依赖注入？
用容器管理依赖。

```javascript
class Container {
  constructor() {
    this.dependencies = new Map();
  }
  
  register(key, value) {
    this.dependencies.set(key, value);
  }
  
  get(key) {
    return this.dependencies.get(key);
  }
}

const container = new Container();
container.register('log', () => console.log('Hi'));
container.get('log')(); // "Hi"
```

---

### 68. 如何用JS实现一个简单的队列？
用数组模拟。

```javascript
class Queue {
  constructor() {
    this.items = [];
  }
  
  enqueue(item) {
    this.items.push(item);
  }
  
  dequeue() {
    return this.items.shift();
  }
  
  peek() {
    return this.items[0];
  }
  
  isEmpty() {
    return this.items.length === 0;
  }
}
```

---

### 69. 如何实现一个简易的节流 Hook？
React中节流Hook。

```javascript
import { useRef, useCallback } from 'react';

function useThrottle(callback, delay) {
  const last = useRef(0);
  
  return useCallback((...args) => {
    const now = Date.now();
    if (now - last.current >= delay) {
      callback(...args);
      last.current = now;
    }
  }, [callback, delay]);
}
```

---

### 70. 如何用JS实现一个简单的观察者模式 Hook？
React中观察者Hook。

```javascript
import { useState, useRef } from 'react';

function useObserver() {
  const [state, setState] = useState(null);
  const subscribers = useRef(new Set());
  
  const subscribe = (fn) => {
    subscribers.current.add(fn);
    return () => subscribers.current.delete(fn);
  };
  
  const notify = (data) => {
    setState(data);
    subscribers.current.forEach(fn => fn(data));
  };
  
  return { state, subscribe, notify };
}
```

---

## 终极篇：模式与架构

### 71. `Array.prototype.at` 有什么特别之处？
`at` 支持负索引访问数组元素。

```javascript
const arr = [1, 2, 3];
console.log(arr.at(1)); // 2
console.log(arr.at(-1)); // 3（最后一个）
```

---

### 72. 如何实现一个安全的JSON解析器？
用try-catch包装。

```javascript
function safeParse(jsonStr, fallback = {}) {
  try {
    return JSON.parse(jsonStr);
  } catch (e) {
    console.error('Parse failed:', e);
    return fallback;
  }
}

console.log(safeParse('{"a": 1}')); // { a: 1 }
console.log(safeParse('oops')); // {}
```

---

### 73. 什么是 `Temporal` API？
`Temporal` 是提案中的日期时间API，解决 `Date` 的历史遗留问题。

```javascript
// 未来用法（提案阶段）
// const now = Temporal.Now.plainDateTime('gregory');
```

目前可用 `luxon` 或 `day.js` 替代。

---

### 74. 如何实现一个简易的命令模式？
命令模式封装请求为对象。

```javascript
class Command {
  constructor(execute, undo) {
    this.execute = execute;
    this.undo = undo;
  }
}

const calculator = {
  value: 0,
  add(x) { this.value += x; },
  subtract(x) { this.value -= x; }
};

const addCmd = new Command(
  () => calculator.add(5),
  () => calculator.subtract(5)
);

addCmd.execute(); // value: 5
addCmd.undo(); // value: 0
```

---

### 75. 如何用JS判断变量是否为整数？
用 `Number.isInteger`。

```javascript
console.log(Number.isInteger(42)); // true
console.log(Number.isInteger(3.14)); // false
console.log(Number.isInteger(NaN)); // false
```

---

### 76. 如何实现一个简易的策略模式？
策略模式根据条件选择算法。

```javascript
const strategies = {
  fast: (n) => n * 2,
  slow: (n) => n + 1,
};

function compute(type, value) {
  return strategies[type](value);
}

console.log(compute('fast', 5)); // 10
console.log(compute('slow', 5)); // 6
```

---

### 77. 如何用JS实现一个简易的观察者队列？
观察者队列按顺序执行。

```javascript
class ObserverQueue {
  constructor() {
    this.queue = [];
  }
  
  add(fn) {
    this.queue.push(fn);
  }
  
  notify(data) {
    this.queue.reduce((prev, fn) => 
      prev.then(() => fn(data)), Promise.resolve()
    );
  }
}
```

---

### 78. 如何实现一个简易的适配器模式？
适配器模式转换接口。

```javascript
const oldAPI = { getData: () => ({ x: 1 }) };

const newAPI = {
  fetch() {
    const data = oldAPI.getData();
    return { value: data.x };
  }
};

console.log(newAPI.fetch()); // { value: 1 }
```

---

### 79. 如何用JS判断浏览器是否支持某个特性？
用特性检测。

```javascript
function supportsWebSocket() {
  return 'WebSocket' in window;
}

console.log(supportsWebSocket()); // true或false
```

---

### 80. 如何实现一个简易的工厂模式？
工厂模式封装对象创建。

```javascript
class Product {
  constructor(type) {
    this.type = type;
  }
}

function factory(type) {
  return new Product(type);
}

const p1 = factory('A');
console.log(p1.type); // "A"
```

---

## 实战与边缘篇

### 81. `Object.freeze` 和 `Object.seal` 的区别
- `Object.freeze`：彻底冻结，属性不可改、删、增
- `Object.seal`：密封，属性不可删、增，但可改值

```javascript
const obj = { a: 1 };
Object.freeze(obj);
obj.a = 2; // 无声失败

const sealed = { b: 1 };
Object.seal(sealed);
sealed.b = 2; // OK
delete sealed.b; // 无声失败
```

---

### 82. 如何实现一个对象不可扩展？
用 `Object.preventExtensions`。

```javascript
const obj = { a: 1 };
Object.preventExtensions(obj);
obj.b = 2; // 无声失败
console.log(obj.b); // undefined
```

---

### 83. `Array.prototype.includes` 和 `indexOf` 的区别
- `includes`：返回布尔值
- `indexOf`：返回索引，找不到返回-1

```javascript
const arr = [1, 2, NaN];
console.log(arr.includes(NaN)); // true
console.log(arr.indexOf(NaN)); // -1
```

---

### 84. 如何优雅地合并多个对象？
用扩展运算符。

```javascript
const a = { x: 1 };
const b = { y: 2 };
const c = { z: 3 };
const merged = { ...a, ...b, ...c };
console.log(merged); // { x: 1, y: 2, z: 3 }
```

---

### 85. 什么是 `BigInt`，怎么用？
`BigInt` 用于处理超大整数。

```javascript
const big = 123456789012345678901234567890n;
console.log(big + 1n); // 123456789012345678901234567891n
```

---

### 86. 如何实现一个简单的防抖 Promise？
防抖加Promise。

```javascript
function debouncePromise(fn, delay) {
  let timer;
  return function(...args) {
    clearTimeout(timer);
    return new Promise(resolve => {
      timer = setTimeout(() => resolve(fn(...args)), delay);
    });
  };
}
```

---

### 87. `Array.prototype.some` 和 `every` 的作用
- `some`：只要一个满足就返回true
- `every`：全满足才返回true

```javascript
const arr = [1, 2, 3];
console.log(arr.some(x => x > 2)); // true
console.log(arr.every(x => x > 2)); // false
```

---

### 88. 如何实现一个简单的状态机？
状态机管理有限状态。

```javascript
class StateMachine {
  constructor() {
    this.state = 'idle';
    this.transitions = {
      idle: { start: 'running' },
      running: { stop: 'idle' }
    };
  }
  
  transition(action) {
    const nextState = this.transitions[this.state][action];
    if (nextState) this.state = nextState;
    return this.state;
  }
}
```

---

### 89. `String.prototype.padStart` 和 `padEnd` 的用途
填充字符串到指定长度。

```javascript
console.log('7'.padStart(2, '0')); // "07"
console.log('cat'.padEnd(5, '-')); // "cat--"
```

---

### 90. 如何实现一个简单的对象观察者？
用 `Proxy` 监听属性变化。

```javascript
function observe(obj) {
  const handlers = {};
  
  return new Proxy(obj, {
    set(target, key, value) {
      target[key] = value;
      if (handlers[key]) handlers[key].forEach(fn => fn(value));
      return true;
    },
    
    get(target, key) {
      if (key === 'on') {
        return (prop, fn) => {
          handlers[prop] = handlers[prop] || [];
          handlers[prop].push(fn);
        };
      }
      return target[key];
    }
  });
}

const data = observe({ count: 0 });
data.on('count', val => console.log('Count:', val));
data.count = 1; // "Count: 1"
```

---

### 91. 如何用JS获取浏览器窗口大小？
用 `window.innerWidth/Height`。

```javascript
function getWindowSize() {
  return {
    width: window.innerWidth,
    height: window.innerHeight
  };
}
```

---

### 92. 如何实现一个简易的装饰器模式？
装饰器模式动态增强功能。

```javascript
function withLog(fn) {
  return function(...args) {
    console.log('Calling with:', args);
    return fn(...args);
  };
}

const add = (a, b) => a + b;
const loggedAdd = withLog(add);
console.log(loggedAdd(1, 2)); // "Calling with: [1, 2]" 3
```

---

### 93. 如何用JS判断网络状态？
用 `navigator.onLine`。

```javascript
function isOnline() {
  return navigator.onLine;
}

// 监听网络变化
window.addEventListener('online', () => console.log('online'));
window.addEventListener('offline', () => console.log('offline'));
```

---

### 94. 如何实现一个简易的管道函数？
管道函数按顺序处理数据。

```javascript
function pipe(...fns) {
  return (x) => fns.reduce((v, f) => f(v), x);
}

const double = (x) => x * 2;
const addOne = (x) => x + 1;
const result = pipe(double, addOne)(5); // 11
```

---

### 95. 如何用JS获取URL参数？
解析 `location.search`。

```javascript
function getQueryParams() {
  const params = new URLSearchParams(location.search);
  return Object.fromEntries(params);
}

// URL: ?a=1&b=2
console.log(getQueryParams()); // { a: "1", b: "2" }
```

---

### 96. 如何实现一个简易的防抖队列？
防抖队列按顺序执行。

```javascript
function debounceQueue(fn, delay) {
  let queue = [];
  let timer;
  
  return function(...args) {
    queue.push(args);
    clearTimeout(timer);
    timer = setTimeout(() => {
      fn(queue);
      queue = [];
    }, delay);
  };
}
```

---

### 97. 如何用JS判断文件类型？
解析文件扩展名。

```javascript
function getFileType(fileName) {
  return fileName.split('.').pop();
}

console.log(getFileType('image.png')); // "png"
```

---

### 98. 如何实现一个简易的节流队列？
节流队列限制频率。

```javascript
function throttleQueue(fn, delay) {
  let queue = [];
  let last = 0;
  let timer;
  
  return function(...args) {
    queue.push(args);
    const now = Date.now();
    
    if (!timer) {
      timer = setInterval(() => {
        if (queue.length) {
          fn(queue.shift());
          last = Date.now();
        } else {
          clearInterval(timer);
          timer = null;
        }
      }, delay);
    }
  };
}
```

---

### 99. 如何用JS实现一个简易的负载均衡？
随机分配任务。

```javascript
function loadBalance(tasks, workers) {
  return tasks.map(task => ({
    task,
    worker: workers[Math.floor(Math.random() * workers.length)]
  }));
}

console.log(loadBalance([1, 2, 3], ['A', 'B']));
```

---

### 100. 如何用JS实现一个简易的错误重试机制？
自动重试失败操作。

```javascript
async function retry(fn, maxAttempts, delay) {
  for (let i = 0; i < maxAttempts; i++) {
    try {
      return await fn();
    } catch (e) {
      if (i === maxAttempts - 1) throw e;
      await new Promise(r => setTimeout(r, delay));
    }
  }
}

retry(() => fetch('https://example.com'), 3, 1000)
  .then(console.log)
  .catch(console.error);
```

---

## 💎 结语

恭喜你完成了这100个JavaScript核心概念的梳理！从基础到深入，从原理到实践，希望这份指南能成为你面试备战和日常开发的得力助手。

**记住**：理解原理远比死记答案更重要。尝试自己动手实现一遍文中的手写代码，并思考它们在实际项目中的应用场景。

学习之路永无止境。在掌握这些之后，你可以进一步探索：
- **框架原理**（如React/Vue的响应式、虚拟DOM）
- **工程化**（Webpack/Vite、Babel、代码规范）
- **Node.js** 与全栈开发
- **性能优化**与浏览器渲染原理

如果对某个知识点有疑问，或想深入探讨，欢迎留言交流。祝你下一场面试顺利，拿到心仪的Offer！🌟

---
**最后**：整理这100题确实是个大工程，如果觉得有帮助，别忘了点赞或分享给你的小伙伴哦！ 😄
